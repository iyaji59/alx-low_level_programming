#include "main.h"
/**
 * set_string - sets the value of a pointer
 * @s: source address
 * @to: target address
 * Return: we are not making any return
 */
void set_string(char **s, char *to)
{
	*s = to;
}
